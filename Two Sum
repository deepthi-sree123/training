class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        
        seen = dict()
        for idx,val in enumerate(nums):
            c = target - val
            if c in seen:
                return [seen[c],idx]
            seen[val] = idx

        











